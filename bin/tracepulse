#!/bin/sh
# TracePulse CLI launcher - resolves symlinks for ESM compatibility.
# Fixes npm global install where symlinked ESM files break import.meta.url.
exec node "$(dirname "$(readlink -f "$0" 2>/dev/null || realpath "$0" 2>/dev/null || echo "$0")")/../dist/cli.js" "$@"
